#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	// C programlamada tek satırlık yorumlar bu şekilde yazılır
	
	/*.birden fazla satırlık yorumlar için kullanılır
	.
	.
	*/ 
	/* c programlamada değişkenler şu şekilde tanımlanır 
	DEĞİŞKEN TİPİ DEĞİŞKEN ADI ; ŞEKLİNDE
	
	int x;
	integer değeri çıktısını---> printf("%d",x);
	
    float y = 3.14;
	float değerin çıktısını---> printf("%f",y);
	
	double değişkeni ise büyük sayılar için kullanılır
	double z = 2534545345345345345;
	double değerinin çıktısını ise şu şekildedir;
	printf("%f",z);
	
	char veri tipi ise karakterler için tanımlanır 
	
	char m = 'M';
	printf("%c",m);----> ekrana m değişkenini basar
	printf("%d",m);----> ekrana karakterin ascıı değerini basar
	
	%s ise karakter dizilerini  yazmak için kullanılır
	karakter dizisi ise birden fazla karakterin bir araya gelmesidir örn; "mustafa" ve çift tırnak kullanılır
	
	char ad = "Mustafa";
	printf("%s",ad);
	
	
	*/
	return 0;
}
